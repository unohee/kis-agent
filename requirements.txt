# 핵심 의존성 (PyPI 배포용)
requests>=2.26.0
pandas>=1.3.3
python-dotenv>=0.19.2
python-dotenv>=0.19.0
pycryptodome>=3.23

# 개발 의존성 (선택사항)
pytest>=7.0.0
pytest-cov>=3.0.0
black>=22.0.0
isort>=5.10.0
flake8>=4.0.0
mypy>=0.910

# 웹소켓 기능 (선택사항)
# websockets>=12.0
# aiohttp>=3.9.0
