# pykis 개발 가이드

## 프로젝트 개요
pykis는 한국투자증권 API를 Python으로 쉽게 사용할 수 있도록 래핑한 라이브러리입니다. 본 프로젝트는 pip를 통해 설치하여 사용할 수 있는 형태로 개발되고 있습니다.

## 핵심 컴포넌트
- `Agent`: 메인 래퍼 클래스 (Facade 패턴)
- `Client`: API 통신을 담당하는 기본 클라이언트
- `StockAPI`: 주식 관련 API 기능
- `ProgramTradeAPI`: 프로그램 매매 관련 API 기능
- `ConditionAPI`: 조건검색 관련 API 기능
- `AccountAPI`: 계좌 관련 API 기능

## 주요 기능
<<<<<<< HEAD
1. 계좌 관리 (v0.1.26에서 대폭 확장)
   - **기본 조회**: 계좌 잔고, 주문 가능 금액, 총 자산 조회
   - **주문/체결**: 현금 매수/매도, 주문 정정/취소, 일별 주문체결 조회
   - **예약주문**: 예약주문 등록/조회/정정/취소
   - **신용거래**: 신용 매수/매도, 신용매수가능 조회
   - **손익분석**: 기간별 매매손익, 실현손익 잔고 조회
   - **증거금관리**: 통합증거금 현황, 기간별 권리현황 조회
=======
1. 계좌 관리
   - 계좌 잔고, 주문 가능 금액, 총 자산 조회
   - 주문 내역 및 체결 내역 조회
   - 신용 주문, 주문 정정/취소, 예약 주문
>>>>>>> a49c093ae6bced46934b90424a2ff3dac213c4ba
2. 주식 시세 조회
   - 국내주식: 현재가, 일별시세, 호가정보, 분봉 데이터
   - 해외주식: 기간별시세, 뉴스, 권리정보
   - 채권: 기간별시세
3. 시장 분석
   - 체결강도 순위 (올바른 API 적용)
   - 등락률 순위 (국내주식 등락률 순위 API)
   - 거래량 순위
   - 투자자별 매매 동향
   - 증권사별 투자의견
4. 재무정보
   - 손익계산서
   - 재무비율
   - 성장성/안정성 지표
5. 프로그램 매매 정보 조회
   - 종목별 프로그램 매매 추이
   - 시장 전체 프로그램 매매 현황
   - 독립적인 분석 도구 (`examples/program_trade_analysis.py`)
6. 조건검색식 종목 조회 (통일된 API 방식)
7. 휴장일 정보 조회 (직접 API 접근 + 편의 메서드)
8. 실시간 시세 조회

<<<<<<< HEAD
## 최근 주요 개선사항 (v0.1.26)
1. **계좌 관련 API 대폭 확장 (2025-01-08)**
   - **15개 신규 API 추가**: koreaInvest.xlsx 파일의 모든 계좌 관련 엔드포인트 구현
   - **주문 관리 강화**: 현금/신용 주문, 정정/취소, 예약주문 전체 지원
   - **손익 분석 도구**: 기간별 매매손익, 실현손익 조회로 투자 성과 분석 가능
   - **Agent Facade 패턴**: 모든 API를 Agent 클래스에서 직접 호출 가능

## 이전 주요 개선사항 (v0.1.21)
=======
## 최근 주요 개선사항 (v0.1.21)
>>>>>>> a49c093ae6bced46934b90424a2ff3dac213c4ba
1. **API 안정성 및 정확성 대폭 향상**
   - **계좌 API 엔드포인트 수정**: `get_cash_available` (TTTC8908R), `get_total_asset` (CTRP6548R) 올바른 API 적용
   - **메서드명 오류 수정**: `get_minute_chart` → `get_minute_price` 존재하지 않는 메서드 호출 문제 해결
   - **문법 오류 수정**: `pykis/websocket/client.py` 들여쓰기 오류 해결로 모든 모듈 정상 작동
   - **테스트 시스템 100% 통과**: 127개 테스트 성공, API 신뢰성 완전 보장

2. **헬퍼 모듈 및 테스트 자동화**
   - **`examples/test_helpers.py` 모듈 분리**: 재사용 가능한 테스트 헬퍼 함수들 모듈화
   - **주피터 노트북 v2.0**: 헬퍼 모듈 활용으로 코드 재사용성 및 유지보수성 향상
   - **RTX_ENV 가상환경 완전 검증**: 모든 기능 정상 작동 확인

## 이전 주요 개선사항 (v0.1.16)
1. **KOSPI200 지수 베이시스 계산 기능 추가**
   - `get_kospi200_futures_code` 함수: 오늘 날짜 기준 최근월물 자동 계산 (두 번째 주 목요일 만기 규칙 적용)
   - 현재 날짜를 기준으로 다음 만기월(3,6,9,12월) 중 가장 가까운 미래 월물 계산
   - 매월 두 번째 주 목요일을 만기일로 정확히 계산하여 만기일 지난 경우 다음 만기월 반환
   - KOSPI200 선물 종목코드 자동 생성 (예: 101S12, 201S03 등)
   - `get_future_option_price` 메서드의 기본값에 자동 적용
   - 별도 파라미터 없이 호출해도 최신 KOSPI200 선물 시세 자동 조회

## 최근 주요 개선사항 (v0.1.15)
1. **선물옵션 시세 API 추가**
   - `get_future_option_price` 메서드: 지수선물, 지수옵션, 주식선물, 주식옵션 시세 조회 지원
   - 시장분류코드별 조회: F(지수선물), O(지수옵션), JF(주식선물), JO(주식옵션)
   - 종목코드 형식: 선물 6자리(예: 101S03), 옵션 9자리(예: 201S03370)
   - 기본값: 지수선물(F), 종목코드(101S09)

## 최근 주요 개선사항 (v0.1.14)
1. **업종 시세 API 추가**
   - `get_daily_index_chart_price` 메서드: 국내주식업종기간별시세 조회 기능
   - 업종별 시세 데이터 조회 (종합, 대형주, 중형주, 소형주, KOSPI, KOSDAQ 등)
   - 기간별 조회 지원 (일봉, 주봉, 월봉, 년봉)
   - 최대 50건 데이터 수신, 연속 조회를 위한 날짜 범위 설정 가능

## 최근 주요 개선사항 (v0.1.13)
1. **체결 관련 API 확장**
   - `get_hourly_conclusion` 메서드: 시간대별 체결 조회 기능
   - `get_stock_ccnl` 메서드: 주식현재가 체결(최근30건) 조회 - **당일 체결강도 직접 제공**
   - 체결강도 순위권에 없는 종목도 체결강도를 즉시 확인할 수 있어 매매 전략 수립에 유용

## 최근 주요 개선사항 (v0.1.8)
1. **조건검색 API 통일**
   - 모든 조건검색 호출이 `condition.py`의 정확한 방식(`tr_id="HHKST03900400"`) 사용
   - `user_id="unohee"` 매개변수 통일 사용
   - `rt_cd='1'` 응답 정상 처리 개선
   - Facade 패턴 일관성 강화: Agent를 통한 통합 접근

2. **휴장일 기능 추가**
   - `StockAPI`에 `get_holiday_info()` 및 `is_holiday()` 메서드 추가
   - Agent 클래스에 휴장일 기능 통합
   - 직접 API 접근과 편의 메서드 제공
   - 기준일 계산 로직 개선 및 재시도 로직 포함

3. **아키텍처 일관성 개선**
   - Facade 패턴 완전 준수: 모든 기능을 Agent를 통해 접근
   - 내부 API 클래스는 캡슐화하되 적절한 책임 분리 유지
   - 사용자 인터페이스 통일

## 이전 주요 개선사항 (v0.1.4)
1. **API 정확성 향상**
   - 체결강도 API: 올바른 엔드포인트 및 TR 코드 적용
   - 등락률 순위 API: 국내주식 전용 API로 수정
   - 프로그램 매매 API: 종목별/시장별 분리

2. **코드 품질 개선**
   - 중복 메서드 제거 및 통합
   - Strategy 모듈 완전 제거 (deprecated)
   - 조건검색 재귀 호출 문제 해결
   - 로깅 관련 버그 수정

3. **분석 기능 독립화**
   - 프로그램 매매 분석 로직을 별도 스크립트로 분리
   - API 래퍼와 분석 도구의 명확한 역할 분리

## 개발 환경 설정
1. Python 3.8 이상
2. 가상환경 설정 (venv)
3. .env 파일에 API 인증 정보 설정 (.env.example 파일 참조)

## 테스트
- **종합 테스트**: `examples/pykis.ipynb` (50+ 메서드 포괄 테스트, 모듈 재로드 기능 포함)
- 통합 테스트: `kis_integration_test.py`
- 조건검색 테스트: `test_condition_stocks.py` (API 통일 완료)
- 프로그램매매 테스트: `test_program_trade.py`
- 시장정보 테스트: `test_market.py`, `test_market_api.py`

## 주의사항
- API 호출 시 실제 주문이 발생할 수 있으므로 테스트 시 주의
- 인증 정보는 반드시 안전하게 관리
- API 호출 제한에 주의
- 에러 발생 시 로그 확인 필수
- 한국투자증권 OpenAPI 정책 변경에 따라 일부 기능이 영향받을 수 있음
- 조건검색 API는 반드시 올바른 `user_id` 설정 필요

## 개발 가이드라인

# 주석 처리된 사항은 제외된 옵션이다.

0. 해당 저장소를 개발하기 위해서는 가상환경 활성화가 필수적이다. 현 시스템에선 가상환경 활성화 alias인 'venv'를 입력하거나 source ~/RTX_ENV/bin/activate 입력하고 작업을 시작할 것.
<!-- 1. **ContextStrictMode**: 지정된 컨텍스트 블록 외에는 수정 금지 -->
2. **KoreanResponseOnly**: 모든 응답은 한국어로 작성
<!-- 3. **NoNamingAlteration**: 기존 변수명/함수명 유지 -->
4. **NoSilentInference**: 변경 이유를 한국어 주석으로 명시
<!-- 5. **MinimalTouch**: 필요한 최소한의 수정만 실행 -->
6. 모든 API 메서드에 docstring 추가 (목적, 파라미터, 반환값, 예외 처리)
7. 로깅 메시지는 반드시 한국어로 작성
8. 타입 힌트 사용 필수
9. 테스트 코드 작성 및 검증 필수

## 아키텍처 원칙
- **Facade 패턴 준수**: Agent 클래스를 통한 모든 기능 접근
- **API 래퍼와 분석 로직 분리**: 순수한 API 호출과 비즈니스 로직 분리
- **단일 책임 원칙**: 각 모듈은 명확한 단일 책임을 가짐
- **올바른 API 사용**: 한국투자증권 공식 API 문서에 따른 정확한 구현
- **일관된 매개변수**: 조건검색 등에서 통일된 매개변수 사용
- **에러 처리 강화**: 모든 API 호출에 대한 적절한 예외 처리
- **로깅 표준화**: 일관된 한국어 로깅 메시지 사용 